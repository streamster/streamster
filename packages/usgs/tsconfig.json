{
  "extends": "../../tsconfig.json",
  "include": ["src", "types"],
  "compilerOptions": {
    "rootDir": "./src",
    "baseUrl": "./",
    "paths": {
      "*": ["src/*", "node_modules/*"]
    },
    "composite": true
  },
  "references": [
    // Core first because everything needs it
    { "path": "../core" }
  ]
}
